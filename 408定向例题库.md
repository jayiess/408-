# 408 定向例题库

> 用途：根据你的错题和卡点，补同考点、不同问法的例题。  
> 原则：先讲透一道，再做 2 到 3 道变式，不靠题海硬磨。

## 例题来源规则

1. 真题类：优先参考 408 历年真题的题型风格。
2. 仓库类：参考已整理仓库中的真题、笔记、算法代码。
3. 自拟类：我会根据你的薄弱点出同类变式题，并标明“自拟”。
4. 复查类：错题 1 天、3 天、7 天后各安排一次变式复查。

## 已确认可用仓库材料

| 仓库 | 有无例题/题目价值 | 用法 |
| --- | --- | --- |
| https://github.com/CodePanda66/CSPostgraduate-408 | 有，包含 408Exam、OtherExam 和四科资料 | 真题/综合题训练 |
| https://github.com/ddy-ddy/cs-408 | 有，包含王道书和刷题本相关目录 | 一轮配套刷题和查笔记 |
| https://github.com/xusun0623/exam_code_for_408 | 有，2009 到 2024 数据结构算法真题代码 | 算法大题专项 |
| https://github.com/songmuhan/408 | 有，2009 到 2021 真题 PDF | 套卷和真题定位 |
| https://github.com/nnngu/Postgraduate-408-old-exam | 有，真题及部分答案解析 | 真题核对和解析 |
| https://github.com/SSHeRun/CS-Xmind-Note | 主要是笔记/思维导图 | 建框架，少量用于出题依据 |

## 出题模板

每次你说“我不会某题/某知识点”后，我会按这个结构补题：

```text
薄弱点：
原题问题：
补充例题 1：同模型基础题
补充例题 2：同模型变式题
补充例题 3：408 风格综合题
你需要提交：
我批改重点：
```

## 第一批：数据结构开局例题

### 例题 1：复杂度基础

**考点**：时间复杂度，循环次数估算  
**难度**：基础  
**题目**：

设有如下程序段：

```c
int s = 0;
for (int i = 1; i <= n; i *= 2) {
    for (int j = 1; j <= i; j++) {
        s++;
    }
}
```

问：该程序段中 `s++` 的执行次数数量级是多少？

**你先做**：写出每一轮外层循环对应的内层次数，再求和。  
**答案要点**：外层 `i` 取 1, 2, 4, ..., 2^k，且 2^k <= n；总次数为 1 + 2 + 4 + ... + 2^k = 2^(k+1)-1，因此数量级为 O(n)。

### 例题 2：顺序表删除

**考点**：顺序表删除，平均移动次数  
**难度**：基础  
**题目**：

长度为 n 的顺序表，在第 i 个位置删除一个元素，1 <= i <= n。问需要移动多少个元素？若每个位置删除概率相等，平均移动次数是多少？

**你先做**：先固定 i，再对 i 从 1 到 n 求平均。  
**答案要点**：删除第 i 个元素后，需要移动第 i+1 到第 n 个元素，共 n-i 个；平均移动次数为 `(0 + 1 + ... + n-1) / n = (n-1)/2`。

### 例题 3：链表边界

**考点**：单链表删除，头结点，边界条件  
**难度**：中等  
**题目**：

已知带头结点的单链表 `L`，设计算法删除所有值为 `x` 的结点。要求写出核心代码，并分析时间复杂度。

**你先做**：思考为什么要用 `pre` 和 `p` 两个指针。  
**答案要点**：

```c
void DeleteX(LinkList L, ElemType x) {
    LNode *pre = L, *p = L->next;
    while (p != NULL) {
        if (p->data == x) {
            pre->next = p->next;
            free(p);
            p = pre->next;
        } else {
            pre = p;
            p = p->next;
        }
    }
}
```

时间复杂度 O(n)，空间复杂度 O(1)。易错点是删除结点后不能让 `pre` 后移，否则会漏删连续的 `x`。

### 例题 4：循环队列

**考点**：循环队列，判空判满  
**难度**：基础  
**题目**：

循环队列容量为 `MaxSize`，采用少用一个存储单元的方法区分队空和队满。已知队头指针为 `front`，队尾指针为 `rear`，请写出：

1. 判空条件
2. 判满条件
3. 当前队列元素个数

**答案要点**：

```text
队空：front == rear
队满：(rear + 1) % MaxSize == front
元素个数：(rear - front + MaxSize) % MaxSize
```

### 例题 5：二叉树性质预热

**考点**：二叉树结点关系  
**难度**：基础  
**题目**：

一棵非空二叉树中，度为 0 的结点数为 n0，度为 2 的结点数为 n2。证明：n0 = n2 + 1。

**答案要点**：

设总结点数为 n，度为 1 的结点数为 n1，则：

```text
n = n0 + n1 + n2
边数 = n - 1
边数也等于所有结点度数之和 = n1 + 2n2
```

所以：

```text
n0 + n1 + n2 - 1 = n1 + 2n2
n0 = n2 + 1
```

## 之后怎么用

你每发一道错题，我会在这里追加一组“同模型变式题”。  
如果同一类题连续错 2 次，我会把它升级为薄弱专题，安排 3 天复查。

## 第二批：线性表链表指针专题

### 例题 6：已知前驱插入结点

**考点**：单链表插入，指针修改顺序  
**难度**：基础  
**题目**：

在单链表中，已知结点 `pre` 是结点 `p` 的直接前驱，现要在 `pre` 和 `p` 之间插入结点 `s`，应执行哪两条语句？

**答案要点**：

```c
pre->next = s;
s->next = p;
```

如果题目只说“在 p 之后插入 s”，才是：

```c
s->next = p->next;
p->next = s;
```

### 例题 7：循环单链表判空

**考点**：带头结点循环单链表  
**难度**：基础  
**题目**：

设 `L` 为带头结点的循环单链表头指针。请分别写出空表、仅有一个数据结点时的结构关系。

**答案要点**：

```text
空表：L->next == L
一个数据结点：L->next == p 且 p->next == L
```

循环链表里，尾结点不指向 `NULL`，而是指回头结点。

### 例题 8：静态链表插入

**考点**：静态链表链接地址  
**难度**：中等  
**题目**：

静态链表中，结点 A 的地址为 `1000H`，链接地址为 `1008H`；结点 B 的地址为 `1008H`，链接地址为 `NULL`。现将结点 X 存于 `1010H`，并插入 A 与 B 之间。插入后 A、B、X 的链接地址分别是多少？

**答案要点**：

```text
A 的链接地址：1010H
B 的链接地址：NULL
X 的链接地址：1008H
```

插入到 A 和 B 之间，只改 A 和 X；B 原来指向谁，仍然指向谁。
