# 408 考点地图

> 基于 2026 年公开大纲信息整理。当前时间是 2026-05-05，如果你准备的是 2027 考研，正式 2027 大纲通常要等 2026 年下半年发布，届时需要再校对一次。

## 试卷总览

- 满分：150 分
- 时间：180 分钟
- 方式：闭卷、笔试
- 题型：单项选择题 80 分，综合应用题 70 分
- 内容结构：数据结构 45 分，计算机组成原理 45 分，操作系统 35 分，计算机网络 25 分

## 数据结构

主线：逻辑结构、存储结构、基本操作、算法复杂度、C/C++ 实现。

### 必须掌握

- 基本概念：数据结构三要素，抽象数据类型，算法特性，时间复杂度，空间复杂度。
- 线性表：顺序表，单链表，双链表，循环链表，插入删除查找，链表算法题。
- 栈、队列、数组：栈和队列的顺序/链式实现，循环队列，递归，表达式求值，矩阵压缩。
- 树和二叉树：二叉树性质，遍历，线索二叉树，树/森林/二叉树转换，Huffman 树，Huffman 编码，并查集，堆。
- 图：邻接矩阵，邻接表，十字链表，邻接多重表，BFS，DFS，最小生成树，最短路径，拓扑排序，关键路径。
- 查找：顺序查找，折半查找，分块查找，二叉搜索树，平衡二叉树，红黑树，B 树，B+ 树，Hash 表，字符串模式匹配。
- 排序：插入类、交换类、选择类、归并、基数、外部排序；稳定性、复杂度、适用场景。

### 高频题感

- 算法大题常落在线性表、树、图、查找/排序，核心是先说明思路，再写代码，再分析复杂度。
- 图题喜欢把存储结构、遍历序列、最短路径、拓扑/关键路径混在一起考。
- 排序题常考每趟结果、比较次数、移动次数、稳定性和复杂度。

## 计算机组成原理

主线：数据如何表示，指令如何执行，CPU/存储/I-O 如何协同完成程序。

### 必须掌握

- 计算机系统概述：层次结构，存储程序工作方式，性能指标，CPI，主频，CPU 执行时间。
- 数据表示和运算：进制转换，原码/反码/补码，定点数，浮点数，IEEE 754，溢出判断，加减乘除运算电路。
- 存储系统：SRAM，DRAM，Flash，主存与 CPU 连接，多模块存储器，磁盘，SSD。
- Cache：映射方式，替换算法，写策略，命中率，平均访问时间。
- 虚拟存储器：页式、段式、段页式，页表，地址转换，TLB。
- 指令系统：指令格式，寻址方式，大小端，对齐，CISC/RISC，高级语言到机器代码。
- CPU：数据通路，控制器，指令执行过程，异常和中断，流水线，冒险处理，超标量和动态流水线基本概念。
- 总线与 I/O：总线组成、性能、定时，I/O 接口，程序查询、中断、DMA。

### 高频题感

- 组成原理大题常是“地址位数/容量/Cache/页表/TLB/流水线/指令执行”的组合题。
- 计算题要把单位、位数、地址范围、块号、组号、标记位拆清楚。
- 程序与硬件协同是近年重点：C 语句、函数调用、数组访问、Cache 命中、机器指令可能连在一起考。

## 操作系统

主线：OS 如何管理进程、内存、文件和 I/O，并让程序在硬件上安全高效地运行。

### 必须掌握

- OS 基础：基本概念，发展历程，内核态/用户态，中断与异常，系统调用，链接装入，地址空间，OS 结构，引导，虚拟机。
- 进程与线程：概念、状态转换、PCB/TCB、线程实现、进程控制、进程通信。
- 调度：调度层次，调度时机，抢占/非抢占，FCFS，SJF，优先级，时间片轮转，多级队列，多级反馈队列，上下文切换。
- 同步互斥：临界区，软件/硬件方法，锁，信号量，条件变量，生产者消费者，读者写者，哲学家进餐。
- 死锁：必要条件，预防，避免，银行家算法，检测和解除。
- 内存管理：连续分配，分页，分段，段页式，地址变换，内存保护，共享。
- 虚拟内存：请求分页，页框分配，页面置换，缺页率，抖动，工作集，内存映射文件。
- 文件管理：文件逻辑/物理结构，目录，inode，硬链接/软链接，文件系统布局，空闲空间管理，挂载。
- I/O 管理：设备分类，轮询/中断/DMA，I/O 软件层次，缓冲，SPOOLing，磁盘调度，SSD 特性。

### 高频题感

- 操作系统题最爱“状态变化 + 队列 + 算法过程”：调度、银行家、页面置换、磁盘调度。
- PV 操作要先找共享资源和同步顺序，再写信号量初值，最后检查是否死锁。
- 内存管理题要分清逻辑地址、页号、页内偏移、物理块号、物理地址。

## 计算机网络

主线：分层模型下，数据从应用层一路封装、传输、路由、交付到对端。

### 必须掌握

- 网络概述：定义、组成、分类、性能指标，分层结构，协议/接口/服务，OSI 与 TCP/IP 参考模型。
- 物理层：信道、带宽、码元、波特率、比特率，奈奎斯特定理，香农定理，编码调制，交换方式，传输介质，中继器，集线器。
- 数据链路层：组帧，差错控制，滑动窗口，停止等待，GBN，SR，介质访问控制，以太网，IEEE 802.11，VLAN，PPP，交换机。
- 网络层：路由与转发，SDN，拥塞控制，距离向量，链路状态，IPv4，NAT，子网划分，CIDR，ARP，DHCP，ICMP，IPv6，RIP，OSPF，BGP，组播，移动 IP，路由器。
- 传输层：端口，UDP，TCP 报文段，连接管理，可靠传输，流量控制，拥塞控制。
- 应用层：C/S，P2P，DNS，FTP，电子邮件，SMTP，POP3，MIME，WWW，HTTP。

### 高频题感

- 网络计算题集中在时延、带宽、吞吐量、滑动窗口、CRC、子网划分、路由表、TCP 拥塞窗口。
- 分层题要答清楚“本层解决什么问题、提供什么服务、依赖下层什么能力”。
- TCP 题常考三次握手、四次挥手、可靠传输、流量控制、拥塞控制的区别。

## 后续答疑规则

你以后可以直接丢给我：

- 一个概念：例如“TLB 到底干嘛的？”
- 一道题：拍题或贴题干都行。
- 一个章节：例如“操作系统进程同步我学懵了”。
- 一个复盘需求：例如“帮我抽查树和图”。

我会按这个顺序讲：

1. 定位到 408 哪一科、哪一章、哪类题。
2. 先讲直觉，再给正式定义。
3. 画过程或列状态表。
4. 推一遍例题或反例。
5. 最后总结易错点和应试写法。

