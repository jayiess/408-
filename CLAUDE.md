# 408 复习仓库 · 协作规则

## 零、硬性约束：开工口令

**每次干活之前，回复的第一句必须是「好的陈书记。」然后再开始干活。** 没有例外，任何任务、任何长度的回复都要先喊。

这句是**记忆探针**：漏喊说明上下文被稀释、本文件的规则已经开始失效，用户会让你重读 CLAUDE.md。所以一旦发现自己没喊，不要只补这一句，要连带自查其他规则是不是也忘了（尤其是「验证模式不改 md」「答案精简」「不写 `==高亮==`」这几条）。

---

## 零点五、留给下次上线的话

读到这里就要主动跟用户说，不用等他问。**说完（或用户处理完）就把该条删掉**，这里只放没交付的事。

- （当前无）

---

## 目录结构

| 目录 | 用途 |
| --- | --- |
| `王道教材/` | 27 王道四科 Markdown 电子版，只读来源，不要改 |
| `知识点/` | 教材拆出的知识点卡片，命名 `OS-01-章节名.md` / `DS-01-章节名.md` |
| `错题库/` | 错题详解、索引、周复盘 |
| `个人笔记/daily-note/` | 每天随手记 |
| `408学习进度.md` | 四科全章节进度总表 |

用户是二战，目标是快速消化吸收。别铺陈教材原文，给能直接背、能直接用的结论。

---

## 一、拆解知识点

排版模板、跨科交叉引用步骤、KP 粒度标准在 `408-kp` skill 里，拆解前先调用它，别再翻已有卡片对格式。

用户会划选教材片段，说明今天学哪些。收到后：

1. 拆成若干知识点（KP1、KP2……），每个含**核心结论**、**易错点**，教材里的「考点追踪」年份抽成**真题**行。
2. 每个知识点末尾必须附两个空 callout：

```md
> [!note] 我的笔记
> 

> [!question] 我的问题
> 
```

3. 同步更新 `408学习进度.md`：状态、知识点数、卡片链接、最近学习日期。
4. **不要自己打 `==高亮==`**。高亮是用户边读边标的，卡片里一律不写。要强调用 `**加粗**` 或直接写「易错点」「核心结论」。

**不要在卡片里写「本节自测」题目**。自测占篇幅、吃上下文，且题目本身就是 KP 里易错点的重复。要自测时用户会单独提，届时只在对话里出题。

同一章的后续小节追加到同一个卡片文件，KP 编号接着排。

## 一点五、DS / CO 的补卡片阶段

数据结构 2026-06、计算机组成原理 2026-07 已各过一轮，当时没维护仓库，所以没有知识点卡片。

**现阶段这两科也按「一、拆解知识点」逐 KP 拆，产出和 OS 完全一致。** 目的是把缺的卡片补齐，顺带复习。别因为「学过了」就简化成速记表——卡片是后面系统复习和二轮要用的底料，现在偷的工后面得还。

唯一的差别：这两科学过，讲解可以更直接，不用铺陈概念的来由，直接给结论、易错点、真题。用户答自测题的水平也会比新学的高，判错更要抠细节。

等一轮全部补完、进入系统复习阶段时，再换成「必背清单 + 易忘点 + 只给题不给答案的自测」那套快查模式。

## 二、验证笔记（重要）

用户说「学完了」「验证一下」之后：

1. **只验证 `> [!note]` 里笔记的正确性与严谨性**，指出错误、不严谨的表述、漏掉的限定条件。
2. **回答 `> [!question]` 里的问题**。
3. **不修改任何 md 文件**。验证结果和解答只在对话里输出，笔记由用户自己改。

这条没有例外。哪怕发现笔记写错了、格式乱了、有笔误，也只在对话里说，不动文件。

用户明确要求更新笔记时才动手，且优先追加而非覆盖用户原话。

**要求写入答案时的固定格式**：答案写在 `> [!question]` callout 内，紧跟在对应问题的**下一行**，用红字加「答：」前缀。问题原文保持不动，不要挪到笔记 callout 里。

长度：**精简、直切要害**，目标一到两句。只写结论和判据，不写推导过程、不写背景铺垫、不举扩展例子、不加「补：」这类附注。多问多理由的，一句压一条，不要展开。详细讲解放在对话里，卡片里只留能背的那句。

```md
> [!question] 我的问题
> 问题一？
> <span style="color: red;">答：结论。</span>
> 
> 问题二？
> <span style="color: red;">答：结论。</span>
```

## 三、收录错题

**录入走脚本，不要手写 CSV 和章节 md。** 执行流程、JSON 字段、教材选项两种排版的解析、四个坑在 `408-wrong` skill 里，录题前先调用它。

### 入口形态

用户会**划选一整节的习题**（通常是 `x.y.5 本节习题精选` + `x.y.6 答案与解析`），然后只报**题号 + 自己选的答案**：

> 我错了 02、04、05，分别选了 A、C、D

题干、选项、正确答案、解析**全部从划选内容里按题号索引**。不要反问用户题目内容，不要让他重复。划选没覆盖答案区时去 `王道教材/` 对应文件 grep 补齐，**不许猜答案**。

### 两条命令

```powershell
cd F:\二战\408\错题库
.\录入错题.ps1 -Json $j -DryRun   # 先打印给用户确认
.\录入错题.ps1 -Json $j           # 写 错题索引.csv + 对应章节 md
.\更新复习计数器.ps1               # 必须补跑，否则新题在前端没有计数行
```

脚本自动完成：追加 `错题索引.csv`（九列固定顺序）、追加/新建 `错题库/<科目>/<缩写>-章号-章节名.md`、新建文件时同步 `错题库/README.md`。章节路径它自己解析，四科全章节都在内置映射表里。

### 字段与规则

必填七项：`subject`（收 OS/DS/CO/CN 缩写）、`chapter`、`number`（保留前导零）、`topic`、`question`、`answer`、`wrongType`。
常用可选：`myAnswer`、`wrongReason`（渲染成红字）、`reviewConclusion`、`options`、`code`、`questionType`、`date`、`reviewDate`。

**错误类型**（优先从这里选）：概念不清、过程推导错、计算/单位错、审题错、代码边界错、题型陌生、记忆遗漏、时间不够。用户给了自己的错因时，优先保留用户原话再归类。

**复查日期**：脚本默认 date+1 天。旧题手动传 `date` 为题目发生日期。

**图片**：纯文字题不填 `imageSource`；只有图本身参与解题（树形图、图论示意图、矩阵、存储结构表、复杂代码截图）才填。

### 录入后

- 录之前 `grep "<章节>,<题号>," 错题库/错题索引.csv` 确认没录过——脚本**没有重复检测**，同题会录两遍。
- 回复简短说明录了哪几题、改了哪些文件，**详细错因讲解只在对话里讲**，不写进 md。
- 多题同源时，在章节 md 末尾补一段「本次共同错因」。
- 同步更新 `408学习进度.md` 该章的错题行。

---

## 图片规则

- 纯文字题：不保存截图，直接多模态识别题干选项转成 Markdown，不写 `图片来源`。
- 只有图片本身参与解题时才保留来源（树形图、图论示意图、矩阵、存储结构表、复杂代码截图）。
- `王道教材/` 里图片是 `<img src="./pictures/xxx.jpg">` 相对路径，`pictures/` 必须和 md 同级，别改路径也别搬图。

## 其他

- `王道教材/` 是外部仓库内容（Scienmicroit/2027-CSKAOYAN-eBooks 的 Markdown 目录），不建议提交进本仓库 git 历史。
- 用户手动加的 `==高亮==`、表格重排、笔误都保留，不要顺手「修正」。
